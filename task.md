# 任務

1. 專注於 core24 and core26 的開發 (Both arm64 and x86_64), core22 can be postponed.
2. 我要此snap 可以 support clinfo, 所以你應該要在 snapcraft.yaml 的 apps 新稱 clinfo 的相關資訊
3. Please use remote build to build snaps and install the correct snap to each machine based on the LTS.
4. 測試需要完整覆蓋 machines.json 內的所有機器。
5. Record the outcome of clinfo, report to me and record those info in a record.md file